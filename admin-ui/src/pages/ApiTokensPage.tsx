import { KeyRound } from "lucide-react";

import { PlaceholderPage } from "@/pages/PlaceholderPage";

export function ApiTokensPage() {
  return (
    <PlaceholderPage
      title="API Tokens"
      description="Launcher catalog/manifest access tokens."
      icon={KeyRound}
    />
  );
}
